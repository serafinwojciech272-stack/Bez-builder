# BadBuilder

Premium sports-intelligence bet builder prototype built with React, TypeScript and Vite.

Core: canonical sports domain, deterministic odds evaluation, conflict validation, model probability/edge display and responsive builder UI.

The current interface uses mock odds snapshots until a live provider adapter is connected.
